export const RelatedCars = () => {
  return (
    <>
      {/* <h4 className="text-3xl text-center mb-6">سيارات أخرى مقترحة</h4>

      <Swiper
        modules={[Navigation]}
        spaceBetween={20}
        slidesPerView={4}
        grabCursor={true}
        className="!m-0"
        navigation={{
          nextEl: ".related-swiper-next",
          prevEl: ".related-swiper-prev",
        }}
      >
        {[...Array(14)].map((_, index) => {
          return (
            <SwiperSlide key={index}>
              <ProductCard />
            </SwiperSlide>
          );
        })}
      </Swiper>

      <div className="flex items-center justify-center gap-4 mt-6">
        <button className="related-swiper-prev h-fit arrow cursor-pointer bg-[#202324] p-3 rounded-xl text-lg text-white disabled:bg-[#1d2020] disabled:text-[#aaa disabled:cursor-not-allowed">
          <GoArrowRight />
        </button>

        <button className="related-swiper-next h-fit arrow cursor-pointer bg-[#202324] p-3 rounded-xl text-lg text-white disabled:bg-[#1d2020] disabled:text-[#aaa] disabled:cursor-not-allowed">
          <GoArrowLeft />
        </button>
      </div> */}
    </>
  );
};

export default RelatedCars;
